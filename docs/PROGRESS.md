# Project Progress

> Live state of the project. **Read this first in every session.**
> The Snapshot is overwritten after every completed task; the two logs are append-only.

## Snapshot

- **Phase:** Executing M3 (admin UI, Ant Design) — Tasks 10–12 + branding (12b) of 10–15 done
- **Active plan:** `docs/superpowers/plans/2026-06-11-multi-tenant-platform.md` (19 tasks, 4 milestones, ~13h)
- **Last completed:** Task 12 — branding-aware **preview page** at `/tenants/[id]/preview`. Three new tenant-parameterized components: `BrandingFrame` (tenant logo + name bar; recolours AntD controls to the tenant's `primaryColor` via a nested inheriting `ConfigProvider`), `ClaimForm` (controlled, offers only enabled claim types + renders custom fields by type with an exhaustive `never` switch; no submit/API — the page owns those, so Task 15's demo can reuse it), and `ProcessResultPanel` (pure presentation of a `ProcessClaimResult` — the five outputs or the structured errors as an alert, never a crash). The page POSTs `{ tenantId, claim }` to the real **`/api/process-claim`** runtime endpoint (so preview can't drift from runtime — verified by `waitForResponse` in the e2e). Two bugs caught during verification and fixed: (1) the mount effect double-runs under React Strict Mode → the draft is now seeded **once** via a functional guard so a late second load can't wipe an in-progress claim; (2) default/dropdown claim-type order was `Object.keys()` = Postgres **jsonb key order** (by length, DENTAL first) → now derives from the canonical `CLAIM_TYPES` const. Native `<input type="date">` (clean `YYYY-MM-DD`, reliable Playwright `fill`) over AntD's dayjs DatePicker. Verified: `tsc` + `lint` clean, unit **48**, **Playwright 20/20** (5 admin-flows incl. 2 new preview specs + 13 debug-sweep), screenshot confirms the SafeGuard-branded render of the worked example (Auto-approved · Medical receipt · 4× email · SLA 2026-06-19 · operations_manager). Committed: Task 11 was `4f50232`+`1111e66`+e2e-sweep; **Task 12 commit pending user approval.**
- **Also done (Task 12b, branding):** the admin product is now branded **Papaya Keystone** — Papaya's own logo lockup (pink "P" mark + wordmark, `AppHeader`), metadata title, and favicon (`papaya.asia/icon.ico`). `tsc` + `lint` clean, header screenshot confirms; no e2e/runtime dependency on the old "Keystone" name.
- **Next up:** Task 13 — history page (`/tenants/[id]/history`): version rows (no, note, date), "Diff vs current" drawer (`diffConfigs(version, current)` grouped by section), and forward-only rollback (new version noted `rollback to vN`; old rows immutable).
- **Blockers / open questions:** none. (`@prisma/streams-local` warns it wants Node ≥22; we're on Node 20.19 — only affects `prisma dev`'s local server, not migrate/generate/runtime. Revisit if Vercel build complains.) Note: on this dev box port 3000 is held by another project, so the Next dev server runs on **3001**; Playwright config now reads `E2E_PORT` (default 3000) — run the suite with `E2E_PORT=3001 npm run test:e2e` here.

## Decision Log

- 2026-06-11: Project docs live inside the repo under `docs/`, written in English — they are the durable record of a plan-first development workflow.
- 2026-06-11: Durable project state lives in git-tracked files, not session memory — it must survive context loss, session interruption, and machine/directory changes.
- 2026-06-11: Stack: Next.js (App Router) + TypeScript strict + Ant Design + hosted PostgreSQL + Zod shared schemas; deploy on Vercel. Hosted DB chosen because tenant #4 created through the UI must survive restarts/cold starts.
- 2026-06-11: A single `processClaim` engine powers both runtime and preview mode — two implementations would inevitably drift.
- 2026-06-11: Karpathy guidelines adopted as engineering discipline; every plan task carries a `Verify:` criterion.
- 2026-06-11: Tenant config stored as versioned JSONB snapshots (2 tables) — history/diff/rollback become cheap document operations; Zod owns integrity.
- 2026-06-11: Approval tiers stored as strictly ascending upper bounds (overlap/gap impossible by construction); half-open boundary semantics — a boundary amount belongs to the higher tier; `amount < threshold` auto-approves, so threshold 0 auto-approves nothing.
- 2026-06-11: One `/api/process-claim` endpoint serves runtime, preview, and demo page — preview accuracy guaranteed by construction.
- 2026-06-11: No auth (out of challenge scope, documented trade-off); "Reset demo data" re-seed action mitigates public-URL tampering.
- 2026-06-11: Neon chosen for hosted PostgreSQL; Prisma for schema/migrations.
- 2026-06-11: `processClaim` returns the full lifecycle notification plan (all enabled events), not just `claim_submitted`.
- 2026-06-11: Scope = all 8 acceptance criteria + selected enhancements (demo page "one claim three fates", business-day unit tests, branding-aware preview, reset/seed) — fits the committed 10–12h delivery estimate.
- 2026-06-11: Playwright E2E suite added (8 chromium specs, `workers: 1`, reset-demo in beforeAll) as plan Task 17 — covers admin flows unit tests cannot reach; accessible-name selectors only (AntD class names are unstable).
- 2026-06-12: antd v6 instead of the planned v5 — Next 16 ships React 19, which antd v5 only supports via a compat patch; v6 supports React 19 natively. Plan's AntD component mapping (ColorPicker, Select tags, Collapse, etc.) is unchanged in v6.
- 2026-06-12: `--passWithNoTests` added to the test script so the Task 1 verify criterion (exit 0 with zero tests) holds; remove it in Task 2 the moment the first real test file exists, so an empty test run can never silently pass again.
- 2026-06-12: Next 16 scaffold's `AGENTS.md` kept — it points agents at `node_modules/next/dist/docs/` for post-training-cutoff API changes; UI/API tasks must consult those docs.
- 2026-06-12: Validation rule 9 added to spec §8 and plan Task 2 — `autoApprovalThreshold` must be strictly below the first tier's upper bound (when bounded); at/above it the first tier is unreachable (dead config the admin almost certainly didn't intend).
- 2026-06-12: Every schema refinement carries an explicit issue `path` (section/field level), and rejection tests assert `issues[0].path` — this is the error-mapping contract: editor tabs (Task 11) and API 400 bodies (Task 9) locate errors by path, so `path: []` issues are banned.
- 2026-06-12: Claim-input shape is validated with Zod at the API boundary (Task 9), not inside the engine — `processClaim` stays a pure function trusting its typed contract; malformed bodies (garbage `submittedAt`, missing `customFieldValues`) get a 400, never a 500.
- 2026-06-12: Diff treats arrays as atomic leaves (JSON.stringify equality) — element-wise array diffing is out of scope; safe because both diff inputs are always Postgres JSONB reads (consistent key serialization), a precondition documented in the module comment and pinned by tests.
- 2026-06-12: Custom-field types are a named exported enum (`CUSTOM_FIELD_TYPES`/`CustomFieldType`) and the engine's type-validation switch is exhaustive with a `never` default — adding a new field type to the schema fails compilation until its validation is written (silent accept-as-text is impossible).
- 2026-06-12: Shared test fixture `validConfig` lives in `src/lib/config/fixtures.ts` (not in a test file) — importing from test files makes vitest re-execute the imported suite (observed: 48 reported tests instead of 35) and the pattern compounds; test files are not importable modules in this repo.
- 2026-06-12: All four lifecycle notification events (`claim_submitted`/`approved`/`rejected`/`payment_sent`) are enabled for every seed tenant; tenants differ only by channel (SafeGuard email · HealthFirst email+sms · GovHealth email+webhook). The brief states "all four events" only for SafeGuard and leaves event-enablement unstated for the other two — applying it uniformly keeps the demo's contrast purely about channels and avoids inventing per-event on/off rules the brief never specifies.
- 2026-06-12: Seed configs include only the claim types a tenant offers (each `enabled: true`); types a tenant does not offer are absent rather than present-but-disabled. The engine treats absent and disabled identically (`!typeCfg?.enabled`), so e.g. MATERNITY→SafeGuard still yields `CLAIM_TYPE_NOT_ENABLED`; `claimTypes` is a `partialRecord` precisely to allow this minimal representation.
- 2026-06-12: Installed Prisma is **7.8.0**, which is a material change from the plan's v5/v6 assumptions. Verified the v7 setup against official docs before coding: generator `prisma-client` (not `prisma-client-js`) with a **required `output`** (`src/generated/prisma`); the Rust query engine is removed so a **driver adapter is mandatory**; `url` is banned from the schema `datasource` (moves to `prisma.config.ts`); `migrate`/`db push` no longer auto-run `generate`.
- 2026-06-12: Chose **`@prisma/adapter-neon`** (Neon serverless driver over WS/HTTP) over `@prisma/adapter-pg` — it is the idiomatic Vercel+Neon serverless path, sidesteps PgBouncer transaction-mode pitfalls on the pooled connection, and matches why Neon was chosen. Cost: extra deps `@neondatabase/serverless` + `ws` (Node < 22 has no global `WebSocket`, so `neonConfig.webSocketConstructor = ws`) + `dotenv` (for `prisma.config.ts`).
- 2026-06-12: Generated Prisma client is **gitignored** (`/src/generated/`) and rebuilt by a `postinstall: prisma generate` script — keeps the repo free of generated code, prevents drift, and guarantees Vercel builds against a fresh client. `DATABASE_URL` is the Neon **pooled** connection (`-pooler` host); the initial migration applied cleanly over it, so no separate `DIRECT_URL` is needed at this size.
- 2026-06-12: The bug-prone versioning/rollback semantics (spec §10) get a **dedicated integration test** against real Neon, rather than only the plan's indirect Task-9 API coverage. It lives in `*.integration.test.ts`, is **excluded from `npm run test`** (which stays pure/offline/deterministic — 41 unit tests), and runs via `npm run test:integration` (own config: `dotenv` setup, serial, 30s timeout for cross-region WS round-trips; scratch `__itest_tenant` cleaned up in `afterAll`). This keeps TDD discipline on the riskiest logic without coupling the unit suite to the network.
- 2026-06-12: API claim-input validation lives in `src/lib/engine/claim-input.ts` (`claimInputSchema`), TDD'd as the one new bit of `lib/` logic; the `process-claim` route parses the body with it before calling the engine, so malformed input (garbage `submittedAt`, non-numeric `amount`, missing `customFieldValues`) returns 400, never a 500 — the engine stays a pure function trusting its typed contract.
- 2026-06-12: The 7 route handlers are thin glue over the already-tested engine/repo, so they are verified by an **end-to-end script against `next dev` + Neon** (real Next 16 routing/runtime) rather than unit-mocked — this catches Next-16-specific issues (async `params`, route registration) that mocks would miss; flow/UI coverage comes later from Playwright (Task 17). Next 16 route specifics confirmed from `node_modules/next/dist/docs` per AGENTS.md.
- 2026-06-12: UI **matches Papaya's brand** (extracted from papaya.asia via the browser): clean white, near-black ink, papaya pink `#ED1B55` accent, neutral grays, peach→pink→violet decorative gradient; **Plus Jakarta Sans** (their typeface) + IBM Plex Mono for figures; pill badges, flat/minimal, 8px radii, Papaya's header format (pink badge + bold wordmark, pink-pill eyebrow, pink CTA). Each tenant's `primaryColor` still supplies per-tenant chroma (card spine + chips). AntD re-themed via `ConfigProvider` tokens + `@ant-design/nextjs-registry`. Chosen over a first "Operational Ledger" draft once the user asked to match the company colours/format — aligning to the employer's brand is a deliberate culture-fit/polish signal.
- 2026-06-12: **Playwright pulled forward** from Task 17 to run per-UI-task (user request): `e2e/` + own config (chromium, `workers: 1`, reset-demo in `beforeAll`, accessible-name/role selectors), `npm run test:e2e`, excluded from unit/integration. UI verification standard = `tsc` + `lint` + `test:e2e` + a screenshot. (Task 17 becomes "consolidate/extend the e2e suite" rather than "introduce it".)
- 2026-06-12: Home tenant list is a **client component** fetching `/api/tenants` (per the plan's "fetch the API, no state library"); the React 19 `react-hooks/set-state-in-effect` lint flags the mount-fetch, but the `setState` runs only after an `await` (deferred, not a synchronous cascade), so it's disabled on that one line with a justification rather than refactored to a server component.
- 2026-06-12: Editor error mapping is one normalized shape (`issues.ts`: paths stringified) consumed identically for client `safeParse` issues and the API's 400 issues — tab badges key off `path[0]`, field messages off path prefixes; after the first failed save the draft re-validates on every change so errors clear as the admin fixes them.
- 2026-06-12: Claim-type toggles in the editor sync SLA entries — enabling a type seeds a 5-business-day default, disabling removes its entry. The SLA tab only renders rows for enabled types, so a stale entry would be invisible-but-invalid (a draft the admin cannot see or fix).
- 2026-06-12: Enabling a notification event the tenant never configured starts with **zero channels** — the inline "needs at least one channel" error forces an explicit choice rather than the UI silently inventing a default channel.
- 2026-06-13: Preview's `ClaimForm` and `ProcessResultPanel` are **controlled + config-parameterized with the submit/API owned by the page**, not the form — so Task 15's demo ("one claim, three fates") can compose three forms around a shared claim and three result columns without forking logic. Both call the same `/api/process-claim` the runtime uses, so preview is correct by construction.
- 2026-06-13: `BrandingFrame` recolours AntD via a **nested `ConfigProvider`** (antd themes inherit, so only `colorPrimary`/`colorInfo` are overridden) — exercising each tenant's brand colour on real controls (the "Process claim" button, focus rings) rather than only storing it. CSS `var(--brand)` carries the same colour to the native date input and frame wash.
- 2026-06-13: Claim-type ordering (default selection + dropdown) derives from the canonical **`CLAIM_TYPES` const, never `Object.keys(claimTypes)`** — config round-trips through Postgres `jsonb`, which normalises object key order by length (DENTAL before OUTPATIENT), so key order is not source order. A latent trap for any UI that iterates a config object read from the DB.
- 2026-06-13: The preview draft is initialised **exactly once** (`setDraft(prev => prev ?? …)`); React Strict Mode double-invokes the mount effect in dev, and a plain set let the second fetch reset an in-progress claim mid-edit (surfaced as a flaky e2e: selected OUTPATIENT/date reverted to defaults). The editor page avoided this only because its draft lives in a child keyed by tenant id; page-level drafts must guard.
- 2026-06-13: Dates use a native **`<input type="date">`** (styled to match AntD; brand-tinted focus ring) instead of AntD's `DatePicker` — it yields the exact `YYYY-MM-DD` string the engine expects with no dayjs/timezone parsing, and is far more reliable to drive in Playwright than a calendar popover.
- 2026-06-13: Playwright config made **port-flexible via `E2E_PORT`** (default 3000, so CI/normal use is unchanged) — this dev box already runs another project on 3000, so our Next dev server lands on 3001; the fixed `baseURL: 3000` made the whole suite silently test the *other* app. Minimal, backward-compatible deviation from Task 17's fixed-port config.
- 2026-06-13 (Task 12b): Admin product branded **Papaya Keystone** using Papaya's own logo lockup + favicon (`papaya.asia/icon.ico`) — it's Papaya's internal multi-tenant config tool, so wearing the employer's identity is a deliberate culture-fit/polish signal; "Keystone" is the product name within Papaya. Papaya's logo is pure CSS (no image asset) so it's reproduced in markup, not downloaded; the colour (`#ED1B55`) and typeface (Plus Jakarta Sans) already matched our theme. Done as a standalone user-requested task with its own commit.
- 2026-06-13 (review pass): Notification display order is fixed **in the engine**, not the panel — `processClaim` now iterates the canonical `NOTIFICATION_EVENTS` const instead of `Object.entries(config.notifications)` (whose order is Postgres jsonb key-length order after a round-trip). Fixing at the single source means preview, the Task 15 demo, and any API consumer all get lifecycle order for free; pinned by a scrambled-key unit test. Same class of jsonb-key-order trap already handled for claim types in `ClaimForm`.

## Session Log

- 2026-06-11 S1: Chose Challenge 15, initialized repo, designed and set up the session-resilient workflow. Next: product spec brainstorm.
- 2026-06-12 S2: Confirmed Task 10 commit landed (`63dffb7`), built Task 11 — six-tab config editor with client Zod validation, field-mapped errors, and 2 new Playwright specs (suite 5/5). Commit pending user approval. Next: Task 12 (preview page).
- 2026-06-12 S2 (cont.): Debugged user-reported save failure → root cause: stale editor tab after demo reset (404 behind a generic toast), not an app bug; added a 13-spec full-feature e2e sweep proving every save path + validation rule. Responsive pass across the admin UI (375/768px verified). Made admin-flows counts DB-relative. Suite: 18/18.
- 2026-06-13 S3: Re-read the challenge brief + internal analyses (workflow & traps) to re-anchor, then built Task 12 — branding-aware preview page (`BrandingFrame` + `ClaimForm` + `ProcessResultPanel`) calling the real `/api/process-claim`. Caught two bugs in verification (Strict-Mode double-load resetting the draft; jsonb key-order picking DENTAL as default) and fixed both. Made the Playwright config port-flexible (another project holds :3000 here; we're on :3001). Suite: 20/20, screenshot confirms the SafeGuard-branded worked example. Task 12 commit pending user approval. Next: Task 13 (history/diff/rollback).
- 2026-06-13 S3 (cont.): Rebranded the admin UI to **Papaya Keystone** (Task 12b) at the user's request — Papaya's logo lockup (pink "P" mark + wordmark in `AppHeader`), metadata title, and favicon from papaya.asia. `tsc` + `lint` clean, header screenshot confirms; no test/runtime depends on the old name. Committed `e7a3d89` (Task 12) + `6464549` (12b), pushed.
- 2026-06-13 S3 (review pass): User flagged a save bug then asked for a review of their Task 12 + branding commits. Live-walked all three tenants in the browser (MCP Playwright): confirmed the `/api/process-claim` 200 call, per-tenant recolour, dynamic custom fields, no-crash error panel. Landed three small fixes on top — engine notification order (canonical, TDD'd, unit 48→49), antd v6 `Alert` `message`→`title` (was a console deprecation error), and `.gitignore` for `/.playwright-mcp/`. Backed out two preview e2e specs I drafted on discovering the user's existing pair already covered it (theirs assert branding + dynamic types too). Suite 20/20. Next: Task 13 (history/diff/rollback).
