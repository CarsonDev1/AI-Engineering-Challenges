# Neon PostgreSQL connection string (use the POOLED connection — host contains "-pooler").
# The runtime connects via the Neon serverless driver adapter; migrations read this too.
DATABASE_URL="postgresql://user:pass@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
